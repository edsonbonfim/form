name: example
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  form:
    path: ..
  provider: ^4.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  effective_dart: ^1.2.4

flutter:
  uses-material-design: true